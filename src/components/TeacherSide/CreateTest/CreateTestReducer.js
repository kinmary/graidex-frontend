const initialState = {

}

export const CreateTestReducer = (state, action) => {
    state = state || initialState;
    switch (action.type) {
        default:
            break;
    };
    return state;
}
